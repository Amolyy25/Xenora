import React from 'react';
import styled from 'styled-components';

const Button = () => {
  return (
    <StyledWrapper>
      <button>
        <a href="#" className="btn2"><span className="spn2">Contact</span></a>
      </button>
    </StyledWrapper>
  );
}

const StyledWrapper = styled.div`

export default Button;
